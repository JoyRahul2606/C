main()
{
	int i=10, j=30, k=30;
	if(i>j && i>k)
	{		printf("i is bigger then other.");	}
	else if(j>k && j>i)
	{		printf("j is bigger then other.");	}
	else if(k>i && k>j)
	{		printf("k is bigger then other");	}
	else
	{		printf("no one bigger then other.");	}
		
}
